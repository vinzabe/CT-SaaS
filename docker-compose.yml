version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile.backend
    container_name: ct-saas-api
    restart: unless-stopped
    environment:
      - PORT=7842
      - ENVIRONMENT=production
      - DATABASE_URL=postgres://grantstorrent:grantstorrent@postgres:5432/grantstorrent?sslmode=disable
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=${JWT_SECRET:-change-this-in-production-use-random-64-chars}
      - DOWNLOAD_DIR=/downloads
      - TORRENT_PORT=42069
    volumes:
      - downloads:/downloads
    ports:
      - "7842:7842"
      - "42069:42069/tcp"
      - "42069:42069/udp"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - ct-saas

  web:
    build:
      context: .
      dockerfile: docker/Dockerfile.frontend
    container_name: ct-saas-web
    restart: unless-stopped
    ports:
      - "7843:7843"
      - "7844:7844"
    depends_on:
      - api
    networks:
      - ct-saas

  postgres:
    image: postgres:16-alpine
    container_name: ct-saas-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_USER=grantstorrent
      - POSTGRES_PASSWORD=grantstorrent
      - POSTGRES_DB=grantstorrent
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U grantstorrent"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - ct-saas

  redis:
    image: redis:7-alpine
    container_name: ct-saas-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - ct-saas

volumes:
  downloads:
  postgres_data:
  redis_data:

networks:
  ct-saas:
    driver: bridge
